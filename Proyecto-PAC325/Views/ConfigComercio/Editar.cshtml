@model Proyecto_PAC325.Models.ConfigComercioModel
@if (TempData["error"] != null)
{
    <div class="mensaje error" id="mensaje">
        <span>@TempData["error"]</span>
        <button id="cerrar-modal">×</button>
    </div>
}
else if (TempData["success"] != null)
{
    <div class="mensaje success" id="mensaje">
        <span>@TempData["success"]</span>
        <button id="cerrar-modal">×</button>
    </div>
}
<section class="bienvenida">
    <div>
        <p>Apartado de Comercio</p>
        <h1>
            ESTE ES EL APARTADO DE COMERCIO DONDE PODRAS ACTUALIZAR LAS CONFIGURACIONES
        </h1>
        <p>Este sector esta designado para que puedas actualizar las configuraciones de los comercios</p>
        <button class="boton bg-oscuro">
            Actualizar Configuración
        </button>
    </div>
</section>

<div class="titulo-container">
    <h2>
        EDICIÓN DE CONFIGURACIÓN
    </h2>
</div>

<div class="act-container">
    <h3>Acciones: </h3>
    <a asp-action="Index">Lista</a>
    <a asp-action="Registro">Registrar</a>
</div>

<section class="registro-container">
    <form class="formulario" asp-action="Update">
        <input type="hidden" asp-for="IdConfiguracion" />
        <input type="hidden" asp-for="IdComercio" />
        <input type="hidden" asp-for="FechaDeRegistro" />
        <label>Tipo de Configuración</label>
        <select class="entrada-select" required asp-for="TipoConfiguracion">
            <option value="1">Plataforma</option>
            <option value="2">Externa</option>
            <option value="3">Ambas</option>
        </select>
        <label>Comisión</label>
        <input type="number" class="entrada-datos" placeholder="Ej: 20" required asp-for="Comision" />
        <label>Estado</label>
        <select class="entrada-select" required asp-for="Estado">
            <option value="0">Inactivo</option>
            <option value="1">Activo</option>
        </select>
        <button class="boton bg-oscuro">
            Actualizar Configuración
        </button>
    </form>
</section>